@model List<Slider>

@foreach (var item in Model)
{
    <div class="swiper-slide">
        <div class="slider-content-area" data-bg-img="~/Upload/Slider/@item.ImgUrl">
            <div class="container">
                <div class="row align-items-center ">
                    <div class="col-12">
                        <div class="slider-content">
                            <h5 class="sub-title">@item.SubTitle</h5>
                            <h2 class="title">@item.Title</h2>
                            <a class="btn-slider" href="shop.html">Indice sifaris et</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}